# Copy to .env.local (gitignored) and fill in from your Supabase project's
# Settings > API page. The anon key is meant to be public — security comes
# from the table's row-level security policies, not from hiding this key.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
